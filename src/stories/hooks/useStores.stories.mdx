import { Meta } from '@storybook/addon-docs';

<Meta title="hooks/useStores" />


# Hook for getting stores from Mobx

```typescript jsx
import { observer } from 'mobx-react'

import { useStores } from 'hooks/useStores'

...

// dont forget about the observer HOC for rerender your updated data
// if you need so
export const SomeComponent = observer(() => {
  
  const { usersStore, uiStore, locationsStore } = useStores()

...

})
```